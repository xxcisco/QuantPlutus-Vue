// 当前项目本地开发已直连后端 API，未启用前端 Mock。
// 若需要恢复 mock，建议改用 vite-plugin-mock（已在 vite.config.js 配置），
// 将 src/mock/services/*.js 改写为符合 vite-plugin-mock 的 `MockMethod[]` 默认导出格式，
// 并设置 .env 的 VITE_ENABLE_MOCK=true 即可。
export default {}
